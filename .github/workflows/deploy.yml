name: Deploy to Dokku

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-api:
    name: Deploy API
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DOKKU_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H steve107.mikrus.xyz >> ~/.ssh/known_hosts

      - name: Push to Dokku (API)
        run: |
          git remote add dokku-api dokku@steve107.mikrus.xyz:course-provisioning-api
          GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519" git push dokku-api main:main --force

  deploy-frontend:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DOKKU_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H steve107.mikrus.xyz >> ~/.ssh/known_hosts

      - name: Push to Dokku (Frontend)
        run: |
          git remote add dokku-frontend dokku@steve107.mikrus.xyz:course-provisioning-frontend
          GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519" git push dokku-frontend main:main --force
